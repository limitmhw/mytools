基于分段缓存的显存-内存异步轮换机制**

1.  **结构定义**：将推理过程中的 Transformer Blocks 划分为**头部（Head）**、**中间（Middle）**和**尾部（Tail）**三个逻辑段。
    *   **Middle Block**：常驻显存，不进行换入换出。
    *   **Head/Tail Block**：在系统内存中维护镜像，用于在显存不足时进行动态交替。
2.  **初始状态**：显存内预装载 Head 与 Middle 段；内存中备份有 Head 与 Tail 段。
3.  **交替调度逻辑**：
    *   **Head 执行阶段**：逐一处理 Head Blocks。每完成一个 Block 的计算，即从显存中释放（销毁）该 Block，同时启动异步 IO，将内存中的 Tail 段预取（Prefetch）至显存。
    *   **切换临界点**：Head 段执行完毕时，显存空间已完全腾挪给异步加载完成的 Tail 段。
    *   **Tail 执行阶段**：确保 Tail 段完全就绪后开始执行。同理，每处理完一个 Tail Block 即行销毁，并同步启动 Head 段的异步回载。
4.  **设计目标**：通过 Middle 段常驻与 Head/Tail 段的异步流水线（Pipelining），实现显存利用率的最大化，并利用计算与通信的重叠（Overlap）掩盖数据传输延迟。

5. 整个流程中显存不会往内存交换数据，用完直接销毁
